"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const fse = require("fs-extra");
const buildSliceDirectoryPath = require("./buildSliceDirectoryPath.cjs");
const writeSliceModel = require("./writeSliceModel.cjs");
function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: "Module" } });
  if (e) {
    for (const k in e) {
      if (k !== "default") {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}
const fse__namespace = /* @__PURE__ */ _interopNamespaceDefault(fse);
const renameSlice = async (args) => {
  const { model: existingModel } = await args.actions.readSliceModel({
    libraryID: args.libraryID,
    sliceID: args.model.id
  });
  await fse__namespace.move(await buildSliceDirectoryPath.buildSliceDirectoryPath({
    ...args,
    model: existingModel,
    absolute: true
  }), await buildSliceDirectoryPath.buildSliceDirectoryPath({
    ...args,
    absolute: true
  }));
  await writeSliceModel.writeSliceModel(args);
};
exports.renameSlice = renameSlice;
//# sourceMappingURL=renameSlice.cjs.map
