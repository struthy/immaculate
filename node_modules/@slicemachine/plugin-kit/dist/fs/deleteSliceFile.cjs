"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const buildSliceFilePath = require("./buildSliceFilePath.cjs");
const deleteProjectFile = require("./deleteProjectFile.cjs");
const deleteSliceFile = async (args) => {
  const filePath = await buildSliceFilePath.buildSliceFilePath(args);
  const relativeFilePath = await buildSliceFilePath.buildSliceFilePath({
    ...args,
    absolute: false
  });
  await deleteProjectFile.deleteProjectFile({
    filename: relativeFilePath,
    helpers: args.helpers
  });
  return filePath;
};
exports.deleteSliceFile = deleteSliceFile;
//# sourceMappingURL=deleteSliceFile.cjs.map
