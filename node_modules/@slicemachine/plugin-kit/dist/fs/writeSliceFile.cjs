"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const buildSliceFilePath = require("./buildSliceFilePath.cjs");
const writeProjectFile = require("./writeProjectFile.cjs");
const writeSliceFile = async (args) => {
  const filePath = await buildSliceFilePath.buildSliceFilePath(args);
  const relativeFilePath = await buildSliceFilePath.buildSliceFilePath({
    ...args,
    absolute: false
  });
  await writeProjectFile.writeProjectFile({
    ...args,
    filename: relativeFilePath
  });
  return filePath;
};
exports.writeSliceFile = writeSliceFile;
//# sourceMappingURL=writeSliceFile.cjs.map
