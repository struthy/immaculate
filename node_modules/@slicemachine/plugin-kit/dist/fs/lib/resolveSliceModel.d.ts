import { SharedSlice } from "@prismicio/types-internal/lib/customtypes";
import { SliceMachineActions } from "../../createSliceMachineActions";
export type ResolveSliceModelArgs = {
    sliceID: string;
    libraryID: string;
    actions: SliceMachineActions;
} | {
    model: SharedSlice;
};
export declare function resolveSliceModel(args: ResolveSliceModelArgs): Promise<SharedSlice>;
