"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const buildSliceDirectoryPath = require("./buildSliceDirectoryPath.cjs");
const deleteProjectFile = require("./deleteProjectFile.cjs");
const deleteSliceDirectory = async (args) => {
  const dir = await buildSliceDirectoryPath.buildSliceDirectoryPath(args);
  const relativeDir = await buildSliceDirectoryPath.buildSliceDirectoryPath({
    ...args,
    absolute: false
  });
  await deleteProjectFile.deleteProjectFile({
    filename: relativeDir,
    helpers: args.helpers
  });
  return dir;
};
exports.deleteSliceDirectory = deleteSliceDirectory;
//# sourceMappingURL=deleteSliceDirectory.cjs.map
