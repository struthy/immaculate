"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const path = require("node:path");
const fsLimit = require("./lib/fsLimit.cjs");
function _interopNamespaceDefault(e) {
  const n = Object.create(null, { [Symbol.toStringTag]: { value: "Module" } });
  if (e) {
    for (const k in e) {
      if (k !== "default") {
        const d = Object.getOwnPropertyDescriptor(e, k);
        Object.defineProperty(n, k, d.get ? d : {
          enumerable: true,
          get: () => e[k]
        });
      }
    }
  }
  n.default = e;
  return Object.freeze(n);
}
const path__namespace = /* @__PURE__ */ _interopNamespaceDefault(path);
const writeProjectFile = async (args) => {
  const filePath = args.helpers.joinPathFromRoot(args.filename);
  let contents = args.contents;
  if (args.format) {
    contents = await args.helpers.format(contents.toString(), filePath, args.formatOptions);
  }
  await fsLimit.mkdir(path__namespace.dirname(filePath), { recursive: true });
  await fsLimit.writeFile(filePath, contents);
  return filePath;
};
exports.writeProjectFile = writeProjectFile;
//# sourceMappingURL=writeProjectFile.cjs.map
