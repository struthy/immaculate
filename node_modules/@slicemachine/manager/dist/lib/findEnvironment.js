const findEnvironment = (environmentDomain, environments) => {
  return environments == null ? void 0 : environments.find((environment) => {
    if (environmentDomain === void 0) {
      return environment.kind === "prod";
    }
    return environment.domain === environmentDomain;
  });
};
export {
  findEnvironment
};
//# sourceMappingURL=findEnvironment.js.map
