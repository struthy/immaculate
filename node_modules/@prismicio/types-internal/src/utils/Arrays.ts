export function toRecord<A extends object>(
	arr: ReadonlyArray<A>,
	key: (obj: A) => string,
) {
	return arr.reduce((acc, entry) => {
		const k = key(entry)
		return {
			...acc,
			[k]: entry,
		}
	}, {})
}
