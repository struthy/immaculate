"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const models_1 = require("@prismicio/api-renderer/lib/models");
const ctx = {
    urlRewriter: {
        optimizeImageUrl(originUrl) {
            return originUrl;
        },
        rewriteImageUrl(view) {
            return view.url || "/mockURL";
        },
        rewriteFileUrl(originUrl) {
            return originUrl;
        },
        rewriteS3Bucket(url) {
            return url;
        },
        enforceCDN(url) {
            return url;
        },
    },
    emptyStringInsteadOfNull: false,
    Extension: {
        DocEncoder: {
            encodeDocId: models_1.Extensions.encodeDocId,
        },
        encoders: models_1.Extensions.IDEncoders,
    },
    LinkResolver: {
        buildUrl(_params) {
            return "/";
        },
    },
};
exports.default = ctx;
