"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const slug_json_1 = (0, tslib_1.__importDefault)(require("../dataset/slug.json"));
const _1 = require(".");
function slug(opts) {
    function exec() {
        const w = randomWord(slug_json_1.default);
        if (!opts)
            return w;
        if ((opts.exactly && w.length != opts.exactly) ||
            (opts.min && w.length < opts.min) ||
            (opts.max && w.length > opts.max))
            return exec();
        return w;
    }
    return exec();
}
exports.default = slug;
function randomWord(dictionnary) {
    const word = dictionnary[(0, _1.randomInt)(0, dictionnary.length - 1)];
    if (!word)
        throw new Error("Error while generating a slug.");
    return word;
}
