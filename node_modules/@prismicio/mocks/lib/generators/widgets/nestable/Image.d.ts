import { ImageContent } from "@prismicio/types-internal/lib/content";
import type { Image } from "@prismicio/types-internal/lib/customtypes";
import type { NestableMock } from "../../Mock";
import type { MockConfig } from "../../MockConfig";
declare type ImageData = {
    width: number;
    height: number;
    url: string;
};
declare type ImageDataset = ReadonlyArray<ImageData>;
export declare function random(def: Image, images?: ImageDataset): ImageContent;
declare type ImageMockConfig = MockConfig<Image["type"], ImageData>;
export declare const ImageMock: NestableMock<Image, ImageContent, ImageMockConfig>;
export {};
