import { type Block } from "@prismicio/types-internal/lib/content";
import { type RenderContext, ApiVersion } from "../../../models";
declare const BlockRenderer: (ctx: RenderContext) => {
    renderBlock(block: Block): (apiVersion: ApiVersion) => unknown;
};
export default BlockRenderer;
