import type { FieldType, SlicesTypes } from "@prismicio/types-internal/lib/customtypes";
export interface Definition<T extends FieldType | SlicesTypes> {
    type: T;
}
export declare function isFieldDef(obj: unknown): obj is WithKey<Definition<FieldType | SlicesTypes>>;
type WithType = {
    type: string;
};
export declare function hasType(obj: unknown): obj is WithType;
export type WithKey<D extends Definition<FieldType | SlicesTypes>> = {
    key: string;
    def: D;
};
export {};
