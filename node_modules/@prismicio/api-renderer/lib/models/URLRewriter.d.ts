import type { ImageContentView } from "@prismicio/types-internal/lib/content";
interface UrlRewriter {
    optimizeImageUrl: (originUrl: string) => string;
    rewriteImageUrl: (view: ImageContentView) => string;
    rewriteFileUrl: (originUrl: string) => string;
    rewriteS3Bucket: (url: string) => string;
    enforceCDN: (url: string) => string;
}
export default UrlRewriter;
