import fetch from './../_node_modules/node-fetch/src/index.js';
import { captureException } from './../_node_modules/@sentry/node/_node_modules/@sentry/core/esm/exports.js';
const sentryFrontendTunnel = async (req, res) => {
  try {
    const envelope = req.body;
    if (envelope) {
      const pieces = envelope.split("\n");
      const header = JSON.parse(pieces[0]);
      const { host, pathname } = new URL(header.dsn);
      const projectId = (pathname == null ? void 0 : pathname.replace(/^\/|\/$/g, "")) ?? "";
      const sentryUrl = `https://${host}/api/${projectId}/envelope/`;
      const response = await fetch(sentryUrl, {
        method: "POST",
        body: envelope
      });
      if (!response.ok) {
        const errorMessage = await response.text();
        throw new Error(errorMessage);
      }
    }
  } catch (e) {
    captureException(e);
  } finally {
    return res.status(202).send();
  }
};
export {
  sentryFrontendTunnel
};
//# sourceMappingURL=sentryFrontendTunnel.js.map
