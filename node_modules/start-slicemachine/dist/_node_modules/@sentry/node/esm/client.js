import * as os from "os";
import { TextEncoder } from "util";
import { ServerRuntimeClient } from './../_node_modules/@sentry/core/esm/server-runtime-client.js';
import { applySdkMetadata } from './../_node_modules/@sentry/core/esm/utils/sdkMetadata.js';
class NodeClient extends ServerRuntimeClient {
  /**
   * Creates a new Node SDK instance.
   * @param options Configuration options for this SDK.
   */
  constructor(options) {
    applySdkMetadata(options, "node");
    options.transportOptions = {
      textEncoder: new TextEncoder(),
      ...options.transportOptions
    };
    const clientOptions = {
      ...options,
      platform: "node",
      runtime: { name: "node", version: global.process.version },
      serverName: options.serverName || global.process.env.SENTRY_NAME || os.hostname()
    };
    super(clientOptions);
  }
}
export {
  NodeClient
};
//# sourceMappingURL=client.js.map
