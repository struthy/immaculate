import path from "node:path";
import { findUpSync } from './_node_modules/find-up/index.js';
import { readPackageSync } from "../read-pkg/index.js";
function readPackageUpSync(options) {
  const filePath = findUpSync("package.json", options);
  if (!filePath) {
    return;
  }
  return {
    packageJson: readPackageSync({ ...options, cwd: path.dirname(filePath) }),
    path: filePath
  };
}
export {
  readPackageUpSync
};
//# sourceMappingURL=index.js.map
