type LoadLocalesConfig = {
    localeIDs?: string[];
} | {
    localeIDs?: string[];
    repositoryName: string;
    accessToken: string;
    fetchFromRepository?: boolean;
};
export declare const loadLocaleIDs: (config: LoadLocalesConfig) => Promise<string[]>;
export {};
