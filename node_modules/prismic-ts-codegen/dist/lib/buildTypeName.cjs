"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const pascalCase = require("pascal-case");
const buildTypeName = (...parts) => {
  let name = pascalCase.pascalCase(parts.filter(Boolean).join(" "), {
    transform: pascalCase.pascalCaseTransformMerge
  });
  if (/^[0-9]/.test(name) ? "_" : "") {
    name = `_${name}`;
  }
  return name;
};
exports.buildTypeName = buildTypeName;
//# sourceMappingURL=buildTypeName.cjs.map
